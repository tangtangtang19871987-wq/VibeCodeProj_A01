# Core: the only hard dependency for examples 00-09 to run.
langgraph>=1.0,<2.0

# Used by the bundled broken_project fixtures' test suites (examples
# 04-06, 08) and by src/verifier.py's run_pytest helper.
pytest>=8.0

# Optional: only used by examples/01_llm_inside_graph when ANTHROPIC_API_KEY
# is set. Every example runs without it, using a clearly-labeled
# deterministic stub instead.
# anthropic>=0.40

# Optional: only used by src/opencode_adapter.py's SDKBackend, the
# preferred way to talk to a real, already-running `opencode serve`
# server (see docs/context_management.md and research/sources.md). Every
# example runs without it via the fake backend.
# opencode-ai>=0.1
